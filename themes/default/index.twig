<!DOCTYPE html>
<html lang="en" class="no-js">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width"/>
	<title>{% if meta.title %}{{ meta.title }} | {% endif %}{{ site_title }}</title>
	{% if meta.description %}
			<meta name="description" content="{{ meta.description|striptags }}">
	{% endif %}{% if meta.robots %}
			<meta name="robots" content="{{ meta.robots }}">
	{% endif %}
	<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Open+Sans:400,700" type="text/css" />
	<link rel="stylesheet" href="{{ theme_url }}/style.css" type="text/css" />
	<link rel="stylesheet" href="{{ theme_url }}/fontello.css" type="text/css" />
	<script src="{{ theme_url }}/scripts/jquery-3.1.1.slim.min.js"></script>
	<script src="{{ theme_url }}/scripts/modernizr-2.6.1.min.js"></script>
	<script src="{{ theme_url }}/scripts/renderer.js"></script>
</head>
<body>
	<div class="pre-header" id="clearfix">
		<div class="left-float">
			<small class="white-text">CCB is launching February 19/2017</small>
		</div>
		<div class="social right-float">
			<a href="https://www.facebook.com/ChristCentralBuffalo/">
				<object type="image/svg+xml" data="{{ theme_url }}/icon-facebook.svg"></object>
			</a>
			<a href="https://christcentralbuffalo.wordpress.com/">
				<object type="image/svg+xml" data="{{ theme_url }}/icon-wordpress.svg"></object>
			</a>
		</div>
	</div>
	<header id="header clearfix">
		<div class="left inner">
			<a class="white-text" href="{{ "index"|link }}" id="logo">
				<img src="{{ theme_url }}/logo-small.png" alt="Christ Central Buffalo's Logo"/>
				<h1 class="white-text"><b>Christ Central<br/>Buffalo</b></h1>
			</a>
		</div>
		<div class="right inner">
			<img class="header-menu-button" src="{{ theme_url }}/menu-icon.png" alt="An interfaceable mobile hamburger button"/>
		</div>
		<nav>
			<ul class="overflow">
				{% for page in pages|reverse if page.title %}
					<li{% if page.id == current_page.id %} class="active"{% endif %}
					   {% if '%2F' in page.url %} id="subdir" {% endif %}>
						<a class="white-text" href="{{ page.url }}"><h3>{{ page.title }}</h3></a>
					</li>
				{% endfor %}
			</ul>
		</nav>
	</header>
    <main id="clearfix">
			<div class="inner content padded">
					{{ content }}
			</div>
			<aside id="clearfix">
			</aside>
    </main>
    <footer id="footer">
			<div class="foot-nav desktop-only">
				<ul class="overflow">
					{% for page in pages|reverse if page.title %}
						<li{% if page.id == current_page.id %} class="active"{% endif %}
							 {% if '%2F' in page.url %} id="subdir" {% endif %}>
							<a href="{{ page.url }}"><h6>{{ page.title }}</h6></a>
						</li>
					{% endfor %}
				</ul>
			</div>
			<div class="inner">
				<span> 	&#169; Christ Central Buffalo 2016</span>
			</div>
    </footer>
</body>
</html>